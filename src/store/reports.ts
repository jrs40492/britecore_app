import firebase from 'firebase/app';
import 'firebase/firestore';

const state = {
  report: 'new report'
};

const getters = {

};

const mutations = {

};

const actions = {
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
